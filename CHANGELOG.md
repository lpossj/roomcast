# Changelog

Roomcast 使用 GitHub Releases 作为主发布记录。本文件保留版本索引，详细内容见 docs/RELEASE_NOTES-<version>.md。

## 0.14.3-beta.2

- 默认网页入口改为固定 HTTPS 站点；分享者和观看者无需注册、登录或设置隧道。生成邀请不再依赖 Cloudflare 边缘连接。
- 适配静态站点子目录，保留网页成员再次分享的完整站点路径。
- 保留现有安全邀请、P2P/VDO/TURN 和采集流程。

## 0.14.3-beta.1

- 桌面房主退出时，给仍在线的网页成员 3 秒响应房间交接探测，避免移动网络稍慢时将房间误判为无人能接管而直接关闭。
- 新增网页房间服务接管测试，覆盖正常响应和延迟 1.5 秒响应的成员。

## 0.14.2-beta.9

- 新增桌面端更新机制：启动后检查新版本（默认开启，可在"设置 → 关于"关闭），发现更新时在设置按钮上显示提示点；设置 → 关于 里的"软件更新"可查看版本、更新说明、检查更新与下载。
- 版本比较遵循语义化版本（`0.14.2-beta.9 < 0.14.2`，`beta.2 < beta.10`），正式版用户不会被推送测试版，草案与非语义化 tag 会被忽略。
- 下载 Windows 产物到系统下载目录并用发布页 `SHA256.txt` 校验，校验不通过不写入文件；不会自动替换正在运行的程序。
- 下载或检查超时时给出明确提示，并始终提供"打开发布页（手动下载）"按钮；该按钮不依赖一次成功的检查结果，网络受限时也能走通。
- 更新检查只请求 GitHub 公开发布接口，不发送任何标识；已同步更新 PRIVACY 与 SECURITY 的边界说明。

## 0.14.2-beta.8

- 修复手机网页切到后台后仍显示在线，导致桌面房主无法退出房间的问题：没有任何成员能应答房间交接时，房主退出改为关闭房间并明确提示，不再抛错卡住；网页端页面后台停留 30 秒后主动退出房间。
- 修复手机网页发送大图后 UI 出现黑屏的问题：暂存前按文件头读出尺寸，长边超过 2000 像素的照片缩放为 WebP（GIF 保持原样），并修正待发送缩略图的元素尺寸。
- 删除网页端实现不了的入口：不提供屏幕采集的浏览器不再渲染共享按钮（此前为禁用的"仅支持观看"占位），设置里也不再显示"音频与采集""本地服务"等无法使用的分类。
- 设置改为二级导航（通用 / 音频与采集 / 网络 / 本地服务 / 关于，按客户端能力裁剪），新增"关于"面板：版本号、作者与反馈渠道、源码仓库、使用声明、数据与隐私边界、许可与第三方组件说明。

## 0.14.2-beta.7

- 首个完整交付"电脑／手机网页观看入口"的版本：桌面端在"分享房间"中自动创建 Cloudflare Quick Tunnel 网页观看链接，离开房间或退出应用后关闭。
- 桌面端不再渲染手机成员栏按钮与关闭按钮；手机网页在不支持 `getDisplayMedia` 时共享按钮显示为禁用的"仅支持观看"。
- 网页端不再渲染仅桌面可用的"窗口模式"按钮；网页建房后提示需要保持页面运行。
- 修复非房主成员重新分享邀请时丢失自建信令地址（`signal`）的问题。
- 通过邀请链接进入时，浏览器禁用存储不再导致白屏。
- 修复单元测试对 `dist/` 的依赖，恢复干净检出上的 `npm run check`（此前会阻断 CI 与发布流程）。
- 新增浏览器运行时回归测试；`verify:release` 增加内置 cloudflared 的存在与 SHA256 校验。
- 新增 `clean:build-scratch`，清理被中断的打包遗留在 `%TEMP%` 的 `ns*.tmp` 工作目录。

## 0.14.2-beta.6

- 修复桌面端误显示手机成员栏按钮和关闭按钮：这两个控件现在只在网页小屏布局渲染，桌面应用完全不渲染它们。
- 网页仅在浏览器提供屏幕采集能力时显示可用的共享操作；不支持 `getDisplayMedia` 的手机浏览器仍可建房、观看和聊天，共享按钮显示为"仅支持观看"。
- 网页端不再渲染只能由桌面应用提供的"窗口模式"小窗按钮。
- 修复非房主成员重新分享邀请时丢失自建信令地址（`signal` 参数）的问题。
- 网页入口支持服务构建产物中的根级资源；补齐打包校验、架构自检、浏览器运行时回归测试与文档。

## 0.14.2-beta.5

- 电脑和手机网页可以创建 P2P 房间；浏览器内运行与桌面端相同的房间规则，不依赖桌面本地 API。
- 修复手机网页默认显示聊天室、成员栏无法打开或关闭的问题。
- 手机网页退出时主动断开房间连接，便于桌面端及时更新成员；浏览器恢复前台时重新声明观看状态并尝试恢复画面。
- 网页端设置不再请求桌面配置接口；本地服务状态只在桌面端显示；网页图片复制和下载改用浏览器能力。

## 0.14.2-beta.4

- 延续并明确小窗置顶开关：默认不置顶，可在小窗控制栏切换。
- P2P 房间的电脑／手机网页观看链接由桌面应用自动创建，内置 cloudflared 2026.9.2 并校验官方 SHA256，不再需要手动上传网页包或另开服务。
- 网页入口只提供打包后的静态页面，设置 CSP、禁止嵌入、禁止 Referrer、禁止 MIME 猜测；邀请密钥放在 URL 片段中并在读取后从地址栏移除。
- 房间结束或退出应用后关闭临时网页入口。

## 0.14.2-beta.3

- 共享小窗默认不置顶，小窗控制栏新增"置顶小窗／取消置顶"。
- 浏览器加入 P2P 房间不再依赖本机 `/api/config`；桌面浏览器在支持 `getDisplayMedia` 时可按房间权限分享。
- 自建 HTTPS PeerJS 信令地址随邀请传给网页观看者；未配置时沿用公共信令。
- 手机浏览器主要用于观看，本版没有实现手机发起屏幕共享。

## 0.14.2-beta.2

- 修复首次入房偶发失败：房主把 ICE 建立（25 秒）与 HMAC 鉴权（8 秒）拆为独立阶段，观看者等待上限与之对齐，不再过早放弃。
- 修复房主丢弃首个屏幕 offer：不再依赖缓存的房间状态，共享存活改用实时 `screenStream.active` 判断。
- 修复采集来源卡片跨引擎残留：枚举前清空来源列表与已选来源 ID。
- 修复 OBS 临时枚举源偶发无法清理：先显式摘除场景项再移除输入，并排除 OBS 已确认移除的延迟销毁对象。
- 修复 OBS 首次准备污染与便携版运行时互删：改用独立 staging 目录，便携版使用每次启动独立的解压目录。
- 未验证边界见 docs/RELEASE_NOTES-0.14.2-beta.2.md 的"已知限制"和 docs/RELEASE_CHECKLIST-0.14.2-beta.2.md。

## 0.14.2-beta.1

- 修正发布版本标识；原 v0.14.1 tag 指向旧提交，已为本次 Beta 使用新版本号。

## 0.14.1 Beta

- 公开测试版基础发布。
- P2P 优先、VDO.Ninja direct fallback、TURN relay-only 兜底。
- OBS 固定帧率采集和 Electron 原生采集。
- 安全、图片、隐私、合法使用和第三方组件文档补齐。
- 发布流程、版本策略、状态与验证边界、自建 PeerJS 文档补齐。

## 0.14.0

- 上一稳定版基线与迁移基础。
