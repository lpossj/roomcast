# Roomcast 工作约定

## 时间戳记录（用户明确要求）

- 每做一步，必须在 docs/时间戳记录/ 的当前任务文档记录实际时间（含时区）、做了什么、依据、遇到的现状、验证方式/结果以及尚未知的范围。
- 新任务使用日期命名的文档，同一任务持续追加；修改、测试、构建、提交、失败与撤回均要记录。
- 不编造时间，不删除旧失败记录，不把历史验证结果当作当前验证；历史交接文档仅作证据，执行范围以当前用户请求为准。
- 归档旧记录时保留内容与原路径入口，不丢失已有本地工作。

## 实现与验证

- 最小修改，不做无关重构；每处修改可独立回滚。
- 不改变TURN/中继的取用方式，不新增中继不可用提示。
- 分析、定位、修改、验证、打包自行完成；仅不可替代的真机操作由用户配合。
- 不重复下载大型发布包；按风险做必要验证，无新增问题不重复有效检查。
- 本地修改、提交和同步不属于发布；push、tag、Release须取得用户明确授权。

## 回答

避免过分夸赞，准确、清晰、有条理；区分事实、推断与未知。仅在缺失信息会实质影响执行范围或结论时提问。
