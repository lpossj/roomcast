# beta.7 发布与线上观看网页更新时间戳

## 2026-09-27 00:09:30 +08:00 — 接收明确发布授权并核对现状

- 用户明确要求升级beta.7、push和Release；同时更新需要修复的线上观看网页，但不发布WebViewer ZIP附件。仅客户端建房，手机网页观看，电脑网页可以在加入后共享但不能建房。该请求已授权本次代码/网页/标签/Release发布，无需重复询问。
- 当前源码main=4360d5f，干净；远端main=0dcec15，gh-pages=d8b710d，beta.7标签/Release不存在。Pages来源gh-pages根目录，legacy构建，当前状态built。
- 保留前轮独立修复提交；本次仅升级版本、发布文档和验证网页能力，TURN/ICE/媒体车道不改。沿用Roomcast架构/测试约定，技能中旧Quick Tunnel建议不应用于当前网络门禁。
- 源码确认canHostRoom仅desktopChrome，handleEnter还阻止网页create；共享能力按getDisplayMedia裁剪。已有邀请/UI修复确需部署新dist才生效。
- 计划使用本机缓存构建beta.7，发布6项附件（EXE/ZIP/source/loopback/SHA256/OBS源码），不构建或上传新WebViewer ZIP。网页通过gh-pages更新，保留.nojekyll及version.json更新降级信息。
- 标签推送会触发旧Release工作流自动重建并clobber附件；本次发布已验证本地产物，将核对并取消仅此次beta.7标签触发的重复工作流，避免二次构建替换验证产物及额外下载。

## 2026-09-27 00:11:00 +08:00 — 版本与发布文档准备

- package.json与lock同步升级0.14.3-beta.7，未自动打标签；新建beta.7说明/验收单，README与Changelog指向新版本。
- 发布流程文档修正：线上观看网页可部署，不等同独立WebViewer附件；仅部署网页入口/静态资源/worklet，不部署Electron更新窗口。version.json应在Release公开后同步。
- 初次README局部行补丁因上下文不是完整原行未匹配，未产生改动；改为精确替换发布说明链接完成。
- 尚未打包、push、Release或部署；将以本版本实际结果勾选验收。

## 2026-09-27 00:14:13 +08:00 — beta.7完整检查与网页能力验证通过

- npm run check退出0：216项测试，215通过、0失败、1可选旧beta.1归档测试跳过；许可证、Vite构建与网络门禁通过。主入口index-C4K3LArW.js，版本beta.7。
- 隔离无窗口Edge验证本版本：手机无采集API时只有加入/观看入口，没有创建/共享；电脑网页保留共享入口，未入房点击它打开加入界面，不能建房。连续hash、popstate、query优先及完整邀请网址预填均通过，页面异常0。
- 页面测试支持对线上同一URL复跑，结果按版本与local/online保存，避免覆盖旧beta.6证据。手机视口仍属模拟，不能代替微信真机。
- 现在开始本地beta.7 EXE/ZIP顺序构建及loopback小组件归档；全部使用本机已校验运行时，不构建WebViewer ZIP。

## 2026-09-27 00:16:09 +08:00 — 发布核对准备与构建现状

- 构建仍在便携EXE压缩，未报失败；小组件归档将在EXE/ZIP顺序完成后执行。
- 复用已修正的包内核对脚本，改为beta.7实际归档及独立结果目录；已准备gh-pages树构建脚本，只读本次dist，发布入口/资源/worklet/nojekyll/版本声明，不包含更新窗口或WebViewer ZIP。部署尚未执行。
- 原关闭回归报告的两个相同basename在备份时会覆盖，已分别另存为beta6-exit-evidence/packaged-SC_CLOSE.json和dev-WM_CLOSE.json，保留旧验证证据。关闭验证只顺序启动测试实例，不触及用户实例。

## 2026-09-27 00:19:09 +08:00 — 本版本产物验证完成，准备推送发布

- EXE/ZIP顺序构建与loopback小组件归档退出0，缓存运行时，无WebViewer ZIP。发布运行时许可证检查通过。
- 本版本verify:release退出0、ok=true、版本beta.7；实际未启用TEST_MODE的打包程序通过卡住请求既有×取消、后续设置可用、下次Escape取消；冻结渲染器且beforeunload否决后SC_CLOSE 70ms/code0，自身7个进程无残留。
- app.asar关键Electron/服务文件4项和全部dist8项逐字节一致；Windows ZIP通过更新器提取的app.asar/主EXE/NOTICE/version与本次目录产物一致。
- EXE SHA256=E89C1B0261626F90F579BF695C8C4A74DE61901143A12C53E09378AF6B72FD52；Windows ZIP=78255D4D05E779323725E17D1427EFA3F61DAD3A3B14F794F9997EC7B9B412D8。
- 已更新本次验收及Release说明实际结果。接下来从干净发布提交生成源码/校验清单、打注释标签、普通push，再建draft上传核对后公开；网页版本声明仅在Release公开后部署。
