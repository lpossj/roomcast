# 手机连接排查接手记录（2026-09-26）

## 2026-09-26 20:50:50 +08:00 — 步骤 1：目标、目录与交接包核对

- 用户确认目标：定位并修复手机连接失败。
- 约束：保持 TURN/中继取用方式；不添加无可用中继提示；最小修改、可单独回滚；每步记载依据与验证；不重复下载发布包；仅不可替代的真机操作需要用户协助；发布前必须取得用户确认。
- 权威源码目录：C:/Users/Administrator/Documents/Deepseek/roomcast-source。
- 验证：源码 main 工作区干净，HEAD 6ac2e75，origin/main fce66c5，领先 5 个提交（交接提示称 4，新增交接文档提交解释差异）；package.json 为 0.14.3-beta.6。
- 聊天工作目录 HEAD 12bd388，较旧，不作为当前实现和发布依据。
- 交接包为 221097 字节；在当前工作区 .test/handoff-20260926 解压阅读副本，并核验所有输出路径在目标目录内；没有下载大型产物。
- 读取并应用 Roomcast 架构与测试规范；当前仅文档与只读核对，未运行测试、未修改代码、未发布。
- 附件中的旧指令作为历史资料；执行范围以当前用户请求和约束为准。

## 2026-09-26 20:53:27 +08:00 — 步骤 2：提出真机 A/B，核对连接差异与网页证据边界

- 已请求同手机、同网络下分别使用现存 beta.5 和 beta.6 新建房间并以各自新邀请加入；保持中继设置不变，收集每版结果、完整错误、系统/浏览器、网络类型及 TURN 开关/测试结果。
- 依据：避免把旧邀请、房间持续时间、单实例仍运行、两个版本混用等因素当成代码回归；main.cjs 保持单实例，切换前需退出；自动检查更新只提示，测试期间不安装更新。
- 验证：git diff v0.14.3-beta.5 HEAD 的 src/ electron/ 仅 main.cjs、p2p.js、useRoom.js 三文件，App.jsx 无差异；beta.3 到 beta.5 的 src/ server/ 仅 App.jsx（入口与更新UI）不同，其余连接/媒体实现相同。
- 本地 origin/gh-pages = d8b710d89c4872f99d370d44d4660047e734b271，与交接中最后部署记录一致；这不是当前线上版本的独立证明。
- 本轮 web 工具无法访问线上站点；本机经指定代理的小文件请求也因 SSL 连接失败，未取得当前线上 version.json/HTML。不宣称已验证线上冻结状态；未下载大型资产。
- 沙箱下 Win32_Process 查询拒绝访问，未确认当前运行的实例路径；不因此操作或结束用户进程。
- 分派三个只读审查：手机报错推断、产物/退出证据、STEP-01…18 历史；结果由主代理集中记录。
- 代码未改动，未重复跑交接里已有全绿验证，未 push/tag/Release。A/B 尚待用户真机结果。

## 2026-09-26 20:57:58 +08:00 — 步骤 3：用户真机反馈与交接结论复审

- 用户反馈：beta.5 与 beta.6 在同一网络均可看到画面；TURN 未启用；流量尚未测试。不同网络下两版的受控对照结果尚缺失。
- 这组结果只证明同网观看可用；不能证明跨网失败原因，也不能证明所有手机版本/浏览器均正常。
- 已独立核对并更正分析边界：首次加入 connect() 先 await openPeer()、connectRemote()，成功后才 room:join；安卓超时出现在 hostReady 未收到 authenticated=true，因此“已进房且鉴权正常”不成立。迁移也复用该函数，需结合提示出现位置判断。
- openPeer 的 open 只说明 Peer 注册曾成功，不保证之后 offer/answer、候选或认证消息双向送达。
- iOS指定 ScreenPlayer 行仅把 VDO失败 reason 交给竞速处理，并非直接 setError(reason)；原始错误码的具体显示来源仍待实际手机页面核实，不可断言VDO服务器/ICE/中继根因。
- 已读指定STEP-01至18，旧发版授权、旧方案与被推翻解释不当作当前指令。
- 发现 RELEASE_CHECKLIST-beta.6 保留撤回前TURN描述、旧bundle与旧EXE/ZIP/source哈希，与现行发布说明、release/SHA256.txt及交接证据冲突。原稿作为历史材料保留；本轮审查记录会明确更正，不把旧勾选项认作最终包验收。
- 用户最新要求：独立审查交接提示中的“不要踩的坑”。已追加审查asar完整性、asar缓存/路径、git代理必要性、package:source洁净要求和Windows子进程参数。
- 初步证据：Windows两个spawn选项可同时设置（当前update-install.mjs亦如此）；libuv另有SW_HIDE与自建Job逻辑，“参数互斥”“Chromium Job必然负责”措辞过度。依据使用官方Node/libuv/Microsoft来源，不只复述旧文档。
- 未改源码/中继/现存app.asar，未下载安装包，未发布。仅当前聊天目录审查文档及隔离小型验证文件可能新增。

## 2026-09-26 21:00:45 +08:00 — 步骤 4：完成“不要踩的坑”独立审查

- 已完成五项提示及手机相关推断的独立核验；结论与依据写入 docs/HANDOFF-AUDIT-20260926.md。
- 证据包括现包EXE fuse/嵌入header哈希、工具源码、隔离321字节ASAR探针、源码包archive提交、两个只读git连接对照、官方Windows/Node/libuv文档。
- 真限制：当前ASAR完整性、源码包默认洁净检查、同路径替换时的头缓存、Windows相对档案路径。
- 需修正：代理必需未证实、detached/windowsHide不是参数互斥、非detached生命周期是libuv自建Job机制而非必然Chromium Job、满一小时不是所有房间必失败、VDO不用TURN不等于手机必失败。
- 额外真实风险：package-source在洁净检查前先删同名现有包；本轮未试跑该命令，已保全现有产物。
- 最终beta.6包无已撤回的refreshRelay与被禁止提示；旧验收清单与最终包不一致已在审查文档中纠正。
- 验证范围：没有重复全量测试、没有重算大包哈希；专项小探针已执行。源码及现有发布资产均未改动，未发布。

## 2026-09-26 21:01:16 +08:00 — 步骤 5：文档保存核验

- HANDOFF-AUDIT-20260926.md 与 MOBILE-SESSION-20260926.md 已保存（分别8478和5967字节，保存时间21:00:45本机时间）；未触碰权威源码。
- 从聊天目录额外以git -C查询源仓库时，沙箱执行账户与仓库所有者不同，Git安全目录检查拒绝。本轮没有修改global safe.directory配置，不将这个权限限制误判为仓库内容或网络故障。
- 审查结论依赖前述成功的只读源码/现包核对，不把这次失败查询写成新的通过验证。
