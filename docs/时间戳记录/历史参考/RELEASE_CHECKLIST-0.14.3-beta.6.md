# Roomcast 0.14.3-beta.6 本地验收记录

更新于 2026-09-26T22:15:49+08:00；替代本轮之前的195/195及2.5秒关闭预算记录。本版仅供本地真实测试，未发布。

## 范围

- 第一次X/Alt+F4直接退出，连接与离房等待可以停止，旧任务不能覆盖新状态。
- TURN取用、ICE政策、邀请内容与移交协议未变，不添加中继提示。
- 自动更新销毁主窗后仍继续，采集收尾不阻塞下载。
- 源码打包拒绝/归档失败保留旧源码包。

## 实际验证

- [x] 205/205全套测试、许可证、构建和网络门禁通过；最终仅无语义缩进修正后补跑定向回归和构建。
- [x] 非测试模式真实Electron：配置请求卡住可停止、取消后设置可用、再次连接Escape取消。
- [x] 冻结渲染器与beforeunload否决下WM_CLOSE/SC_CLOSE直接进程退出，code0；开发SC_CLOSE84ms、自身8个进程无残留。
- [x] 验证脚本注入失败后，finally清理自身完整进程树，避免只杀启动器。用户指出的4个残留测试窗口已核实清理，残留0。
- [x] 最终便携EXE与Windows ZIP顺序构建成功；缓存运行时，无重复下载发布包。
- [x] 实际包欢迎页、桌面桥接、许可证和内置运行时校验通过。
- [x] 实际打包程序不启用TEST_MODE：可取消卡住连接并继续使用，冻结渲染器SC_CLOSE退出及自身进程树无残留。
- [x] app.asar的主进程/服务代码与源码、dist逐字节匹配，含停止操作入口，无撤回的中继警告；ZIP内归档与目录产物一致。
- [ ] 用户真实使用：异常TURN/网页邀请、成员移交失败、采集与音频，以及跨网手机连接。

源码ZIP在此文档最终本地提交后，从干净HEAD生成；来源提交、最终哈希和打包关闭耗时记在release/BETA6-LOCAL-VALIDATION.md及release/SHA256.txt，避免源码包自引用。

## 证据边界

- 本轮不是手机连接/链接生成/移交成功率修复；同网旧beta.5/旧beta.6均可观看、TURN未启用、流量未测，不能推断跨网根因。
- 普通离房仍尝试移交；“停止等待并离开”直接断开。X按用户要求直接结束当前程序与房间，不等待移交。
- 运行时固定Electron44，直接进程退出入口及Windows消息处理在当前版本实测；未来升级须复跑scripts/verify-exit-cancel.mjs。
- 时间戳包含所有失败及修正过程：docs/EXIT-CANCEL-20260926.md。历史打包记录保留，不用旧结果冒充新包。
- 未push、未创建/移动tag、未发布Release或手机静态站点。
