# Roomcast 0.14.3-beta.1 Release Checklist

本版聚焦桌面房主向网页成员移交房间。其余功能沿用上一版本，但不把上一版本的验证视作本版已通过。

## 源码与行为

- [x] 网页接管测试分支由临时哨兵确认实际执行。
- [x] 延迟 1.5 秒的网页接管场景在旧探测等待值下失败，在 3 秒等待值下通过。
- [x] 正常速度网页接管场景通过。
- [x] `npm run check` 通过（164/164 单元测试）。

## 发布资产

- [x] `Roomcast-0.14.3-beta.1-Windows.exe` 与 ZIP 已生成。
- [x] OBS 包、便携版生命周期、采集后端切换、`verify:release` 通过。
- [x] 提交后从 HEAD 生成源码 ZIP、运行时包与严格 SHA256 清单。
- [x] `main` 已推送。
- [ ] 是否创建 `v0.14.3-beta.1` tag 由用户决定；tag 会触发公开发布流程。

## 待真机复核

- [ ] 安卓与 iPhone 浏览器作为唯一接管成员，桌面房主退出后网页成员继续聊天、观看，房间保持可用。
- [ ] 公网慢速与抖动连接、后台挂起及切回前台的交接行为。
- [ ] 完全无响应成员时，房主仍可退出并获得房间关闭提示。
