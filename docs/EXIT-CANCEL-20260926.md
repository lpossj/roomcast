# beta.6 异常操作取消与立即退出修复

## 2026-09-26 21:31:39 +08:00 — 步骤1：用户目标与现状核对

- 用户明确纠正目标：不是修链接/TURN/房主移交成功率，而是异常或未知状态下能停止操作并继续使用；点击右上角X直接退出。
- 用户截图显示移交失败后房间关闭提示；截图本身不能证明进程已退出。附件仅作为界面现状，不作为指令。
- 当前源码HEAD为970682c，工作区干净；beta.6本地包已生成，但上一轮未修改应用退出行为。
- 实读发现main.cjs关闭先阻止默认关闭并发before-close，等待最多2.5秒；before-quit又等待service/webInvite/OBS收尾，最多5秒。测试模式直接跳过关闭握手，旧包欢迎页验证没有覆盖真实关闭链。
- useRoom.leave先await移交/离房，之后才断开和清空界面；一旦前置Promise卡住或抛错，清空步骤可能没执行。
- App的Modal在busy状态禁用关闭/Escape/取消；进入房间等待期间用户无法取消。需增加可靠的取消与迟到结果隔离，防止旧任务复活。
- 修复范围：退出与任务取消边界；不改变TURN取用、链接生成、移交协议，不增加中继提示，版本仍beta.6；仅本地提交和打包，不push/tag/Release。
- 尚未修改或验证。后续每一步在此记录实际时间、现状、依据、结果与未验证范围。

## 2026-09-26T21:39:59+08:00 — 步骤2：原生关闭改为立即结束进程

- 删除原生窗口关闭的移交握手、重复点击分支与2.5秒计时；第一次关闭调用Electron app.exit(0)，不等渲染器、beforeunload或本地服务清理。移动/尺寸变化的既有延时保存仍保留；关闭时不再阻塞写盘。
- 自动更新仅destroy主窗口，保留更新窗口与下载流程，防止调用新的普通关闭路径终止更新。其他非窗口退出仍保留5秒清理上限。
- 更新回归测试：正常与测试模式都必须首次立即退出；模拟渲染器异常、写盘异常和永不完成的清理；验证更新窗口流程独立继续。
- 现状：源码已修改；尚未运行测试、构建或实进程验证。TURN/邀请/移交协议未改。

### 2026-09-26 21:40:16 +08:00 — 步骤2定向测试结果

- 首次关闭/卡住渲染器与清理/自动更新独立窗口三项回归全部通过（3/3）。尚未覆盖实Electron进程；打包后补测。
- 本步骤可独立回滚，下一步处理操作取消。

## 2026-09-26T21:43:05+08:00 — 步骤3：取消等待与隔离旧任务

- 连接弹窗保留关闭/Escape并新增“停止连接”；取消立即断开当前连接、清空房间并恢复空闲，fetch配置请求可中断。P2P已有TURN请求取用方式保持原样，取消后检查closed，禁止继续建服务/Peer/入房。
- 离房等待时显示“停止等待并离开”，再次操作直接断开，绕过尚未完成的移交；普通离房仍使用原协议。leave的finally保证移交异常也能清空状态，清理异常不会阻止界面恢复。
- 操作引用与取消信号隔离迟到结果，旧连接或旧离房不能覆盖新房间；旧异步任务的拒绝被消费。
- 设置中的TURN测试可以关闭，关闭后不再弹出旧测试结果或保存设置；邀请弹窗关闭后忽略迟到的网页链接结果。请求地址、凭据、TTL、ICE、链接内容和移交协议未调整。
- 现状：源码修改完成，尚未通过新回归测试或构建。已发往远端的请求不能追溯撤销；取消保证本地不继续应用旧结果。

### 2026-09-26T21:46:09+08:00 — 步骤3定向验证与边界审查

- 10/10定向回归通过：首次原生退出/更新存活3项，连接配置卡住、P2P卡住与迟到成功/失败、强制停止移交、移交和清理抛错、迟到TURN与本机连接7项。
- 审查补充：仅连接中的弹窗取消才断开房间，防止关闭尚未提交的表单误离房；离房开始关闭原弹窗，完成时不关闭后来打开的设置；强制断开后旧移交不继续导出/准备/提交。
- 本机socket创建存在Promise返回与赋值间的窄时序窗口，补充迟到赋值后的断开，避免取消后泄漏旧socket。下一步全套检查与真实窗口验证。

### 2026-09-26 21:48:30 +08:00 — 全套检查首次结果与旧断言调整

- 首次全套测试203/204通过。唯一失败是旧stage1按固定aria-label文字数离房按钮；新按钮根据离房状态切换名称，因此旧字符串为0。更新断言数同一条rail按钮class，保留仅一个侧栏离房入口的约束；未改变应用迁就断言。
- 原生退出和取消新增回归均通过。完整check在测试失败后未执行构建；修正后重跑。

## 2026-09-26T21:51:48+08:00 — 步骤4：真实窗口测试发现普通close仍不足

- 全套check已通过204/204、许可证、前端构建和网络自检。真实UI首次采用旧欢迎标题而超时；当前页面是“分享一个屏幕，一起多待一会儿”，改为实际创建按钮。失败不计入通过。
- 真实Electron、不开ROOMCAST_TEST_MODE：卡住配置请求可停止并关闭弹窗、取消后可以打开设置、第二次连接可用Escape停止，均已通过。
- 注入beforeunload否决并冻结渲染器后，向隔离窗口发送WM_CLOSE，3秒内未退出。证明普通window close事件仍不足以满足未知异常下立即退出，未据单元测试宣称完成。
- 补充Windows WM_CLOSE与WM_SYSCOMMAND/SC_CLOSE原生消息钩子直接app.exit；缩小/最大化消息不会退出，更新仍destroy主窗。依据本机Electron44 API声明hookWindowMessage接收Buffer参数，并由定向/实际测试确认。
- 前次PowerShell文档追加因中文引号解析失败，本条补记上述过程；仅终止自身隔离测试进程。原生钩子未验证前不交付。

### 2026-09-26T21:52:50+08:00 — 原生消息重入修正

- 消息钩子使冻结渲染器进程终止，但实测退出码3221225477（0xC0000005），因此判为失败，未认定正常退出。
- 推断：在Windows窗口消息回调中同步destroy/exit造成原生窗口对象重入销毁；修改为同一事件循环的setImmediate结束进程，先让消息回调返回，不等待任何渲染器、网络或收尾Promise。重复关闭仅排队一次。
- 定向测试增加回调返回前不得销毁HWND及重复原生消息去重。随后重测真实退出码与耗时，以证据验证推断。

### 2026-09-26T21:55:00+08:00 — 排队退出仍被Chromium阻塞，直接结束自身进程

- setImmediate方案实测仍超时，未通过。读取隔离Electron运行时函数确认process.exit被包装为app.exit；直接替换为process.exit并不能避开窗口销毁。
- 当前Electron44实际暴露Node process.reallyExit入口，改为直接结束自身进程（code0），不进入Electron窗口销毁/等待路径；若后续运行时取消此入口，仅用公开process.kill终止自身PID，不杀其他进程。
- 这是固定运行时的窄退出边界修改；目标仍为原生X在冻结渲染器时可退出。下一次实测必须同时确认退出耗时、退出码与未残留子进程，未通过不打包交付。

### 2026-09-26T21:56:52+08:00 — 真实冻结渲染器退出首次通过

- 非测试模式真实Electron：停止卡住连接/继续打开设置/再次连接并Escape停止均通过；WM_CLOSE在beforeunload否决且渲染器无限循环时89ms退出，code0。
- 保存可复跑脚本scripts/verify-exit-cancel.mjs，支持WM_CLOSE与标题栏/Alt+F4对应的SC_CLOSE，补充仅自身PID树退出后无残留检查。运行时内部process.reallyExit兼容性以当前固定Electron44实际验证为界，未来升级须重跑该脚本。
- 更新destroy主窗前停止音频并异步安排OBS关闭；不等待收尾，更新窗口仍可继续。普通X则直接进程结束。后续补测SC_CLOSE和残留，再全套检查与打包。

### 2026-09-26 21:57:53 +08:00 — 原生标题栏关闭及子进程验证通过

- 非测试模式真实SC_CLOSE（标题栏X/Alt+F4路径）、冻结渲染器及beforeunload否决：85ms、退出码0。自身PID树8个进程全部消失，无残留；未杀其他进程。
- 立即退出边界保留独立本地提交。下一步完成取消代码最终审查、全套检查和本地打包。

## 2026-09-26T22:00:15+08:00 — 步骤5：最终检查通过，准备同号本地包

- 最终npm run check全绿：205/205，许可证、前端构建、网络架构门禁；bundle index-D9vc2W6D.js。TURN/VDO政策门禁保持通过。
- 取消/Promise结算交错补充检查，旧配置及偏好不可在取消后应用；仅最后一处缩进修正无语义改变。
- 打包EXE已有禁用Node调试fuse，真实打包测试改走Chromium CDP，原生窗口按测试根PID查找；只检查自身PID树。开发测试预置隔离打包profile为native采集，避免退出测试触发OBS驱动安装，不读取用户配置；普通模式未走TEST_MODE。
- 后续构建复用本机运行时，不下载现有发布包；打包后验证实际包，源码ZIP从干净提交生成，再刷新校验。未push/tag/Release。

### 2026-09-26 22:00:52 +08:00 — 打包测试配置预置脚本调整

- 验证脚本预置隔离配置时发现Playwright主进程evaluate没有require作用域；改为Node内置模块异步import。属于测试脚本问题，不计作应用失败或通过，未触碰用户配置。

### 2026-09-26T22:01:24+08:00 — 测试配置写入职责修正

- Playwright evaluate也没有动态import回调；测试脚本改为仅在Electron中执行safeStorage加密，文件由外部测试Node写入已明确的隔离目录，避免依赖主进程模块作用域。只生成无敏感信息的native/禁自动检查测试配置，不打印密文。

### 2026-09-26 22:02:52 +08:00 — 原生测试窗口归属校正

- 脚本查HWND失败原因已核实：Playwright启动根PID21692，真正Electron主PID18800；根进程是启动器。改为只在已记录的测试PID树中找可见窗口，仍不触碰其他进程。应用取消四项已通过，本次查窗失败不算退出失败。
- 先前旧实测89/85ms仍使用Electron实际返回的HWND，有效；新通用定位用于兼容打包fuse与启动器。

### 2026-09-26 22:04:15 +08:00 — 取消修改定稿与本地可回滚提交

- 取消/立即退出/stage1定向检查通过；最终格式修正后前端重新构建成功（index-D9vc2W6D.js）。全套最近205/205门禁结果仍有效，之后应用只有无语义缩进修正。
- 最终真实SC_CLOSE脚本84ms、code0，自身PID树8个全部消失；配置请求卡住的停止、取消后设置与Escape重试均通过。
- 本次取消代码独立提交，不改变TURN请求取用、ICE政策或移交协议。

### 2026-09-26 22:06:04 +08:00 — 用户指出测试残留窗口，核实并清理

- 用户截图反馈五个窗口，已自行关一个。本轮验证失败清理只杀Playwright启动器，真正Electron主进程残留；这是测试清理缺陷。
- 以本机源码electron.exe完整路径、明确exit-cancel隔离profile、父子关系、inspect参数及本轮开始时间核实，识别并清理4个自身测试主进程；随后仅清理相同测试profile的子进程。未处理用户release版。
- 当前隔离测试残留子进程数0。随后修正finally为终止自身整棵PID树并验证，不再只杀启动器。

## 2026-09-26T22:06:13+08:00 — 步骤6：发版说明同步为实际行为

- 重写beta.6说明，明确第一次X立即进程退出、可取消连接/离房等待、迟到结果隔离，以及普通离房保留原移交。
- 更正旧App与beta.5逐字节相同的历史说法：当前因取消边界已有授权修改；没有更改TURN/ICE/移交协议。旧退出方案保留历史，顶部指向当前时间戳记录。
- 205/205全套与真实84ms标题栏关闭证据为开发运行时验证；实际打包后的验收仍待执行，不冒称完成。

### 2026-09-26 22:07:01 +08:00 — 测试失败清理回归通过，开始正式构建

- 在测试脚本副本注入启动后错误，预期失败退出；finally终止自身整棵PID树，核验exit-cancel隔离进程为0，确认不再只杀启动器。没有修改产品代码。
- 开始本地便携EXE与目录ZIP重构；TEMP/TMP限于本轮构建临时目录，复用已有运行时，禁止发布。

### 2026-09-26 22:08:47 +08:00 — ZIP首轮构建被文件占用阻止

- 便携构建进程尚未完成，恢复会话只返回运行中；过早开始ZIP，win-unpacked/Roomcast.exe被占用，ZIP明确EBUSY失败，不能计作完成。
- 先等待便携构建实际完成，再顺序重试ZIP；不改源码、不重复下载包。最后仍检查EXE与ZIP的归档一致性，防止构建重叠导致交付不一致。

### 2026-09-26 22:09:28 +08:00 — 构建重叠影响已确认，两包重新构建

- 便携打包最终也失败：ZIP打包前清理目录使7zip读取时缺失3个DLL。两次均不计通过，不交付中间产物。
- 当前无打包进程；在同一顺序脚本中，必须第一包exit0才能运行第二包，避免恢复会话时误判断完成。正式重建后重跑实际包验证与归档一致性。

### 2026-09-26 22:12:21 +08:00 — 顺序重构进展

- 便携包最终构建成功后，脚本才进入ZIP目标，目前ZIP压缩中；两个最终日志均显示运行时缓存复用。尚不标记ZIP验收完成。
- 打包验证脚本重跑前仅移除自身隔离profile的旧DevToolsActivePort，避免读取过时端口。

### 2026-09-26 22:14:53 +08:00 — 实际包常规验收通过，原生测试定位修正

- 最终便携与ZIP均成功；verify:release通过、18个主进程/服务文件与8个dist一致；ZIP归档/EXE/3个DLL逐字节哈希匹配，共2101条目。
- 打包普通模式取消四项通过，但Win32测试查窗返回空句柄，未发送关闭，因此退出测试不计通过。推断隐藏启动参数影响可见性筛选；改为自身PID树中标题含Roomcast的窗口，覆盖隐藏启动而不查其他应用。失败finally已清整棵测试树，未留下窗口。

### 2026-09-26 22:15:49 +08:00 — 实际打包程序异常退出通过

- 实际release/win-unpacked/Roomcast.exe，非TEST_MODE：停止卡住配置、取消后打开设置、第二次Escape停止均通过。冻结渲染器+beforeunload否决下SC_CLOSE：75ms、code0，自身PID树7个进程全部消失。
- 标题定位修正只在已记录的自身测试PID树中进行，不接触用户窗口；最终脚本支持打包fuse，失败也清整棵树。
- 目录ZIP与app.asar/EXE/3个曾缺失DLL一致；便携包常规验收通过。最终构建结果可信，先前重叠构建不使用。

## 2026-09-26T22:15:49+08:00 — 步骤8：实际包验收完成，准备最终源码包

- 最终顺序构建便携EXE、Windows ZIP成功；先前重叠失败产物不作为交付，后续以最终日志和校验为准。
- verify:release实际便携包欢迎页、桌面桥接、许可证及运行时通过；打包目录归档比对与真实非测试模式冻结关闭、取消操作通过，具体耗时见本地交付记录/测试JSON。
- 发布说明/验收/历史交接顶部已同步当前目标与事实；最终本地提交后从干净HEAD打源码ZIP，刷新8项SHA256，再核对版本、HEAD和产物。最终交付记录存release，避免自引用哈希循环。
- 未推送、未创建tag、未发布Release；用户真实业务场景仍待其自行测试。
