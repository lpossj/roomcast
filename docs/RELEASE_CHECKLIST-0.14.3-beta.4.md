# Roomcast 0.14.3-beta.4 验收记录

- [x] 全量测试：`npm test` **188/188 通过，0 失败**。
- [x] 构建：`npm run build` 通过；许可证检查与网络架构自检通过。
- [x] **控制台窗口修复有对照实验**：用 Win32 `EnumWindows` 统计可见 `ConsoleWindowClass` 窗口，
  旧方式（`detached: true` + `windowsHide`）期间 **+1～3 个**，新方式（仅 `windowsHide`）**0 个**。
- [x] 回归测试：`the apply script is launched with cmd.exe-safe quoting` 现在断言**不得使用 `detached`**
  且必须 `windowsHide: true`。
- [x] 手动下载按钮移除后无残留引用：渲染层 / 预加载 / 主进程 IPC 的下载与"打开文件位置"通道已删除，
  全仓库扫描仅剩一个无关的测试夹具文件名。
- [ ] **真实更新验证（发布后进行）**：用 `0.14.3-beta.3` 目录版 → 自动更新到本版（`0.14.3-beta.4`）：
  下载 → SHA256 校验 → 解压 → 优雅关闭 → 覆盖程序目录 → 自动重启；
  覆盖后 `resources/app.asar` 的 SHA256 应与发布版资产一致，且**全程不得出现可见控制台窗口**。
  结果见本文件末尾"真实更新验证结果"。
- [ ] 便携版（单文件 EXE）自动更新的真机实测。
- [ ] 覆盖阶段异常矩阵：断电/强制关机、磁盘写满、杀毒软件拦截、只读目录。
- [ ] 未签名产物在干净机器上的 SmartScreen 行为。
- [ ] 真机跨运营商、手机浏览器、VPN 组合、长期弱网与媒体播放完整矩阵（沿用上一版未覆盖项）。

本轮实测范围说明：端到端验证在同一台 Windows 机器上完成，使用发布页线上资产；
**未**重新实测屏幕采集、OBS、音频与媒体播放画面，因为本次未改动这些实现。只把实际完成的检查列为通过。
